# @forge/eval-harness

Promptfoo-based eval harness. Houses golden inputs and expected outputs for each sub-agent (Schema Smith, Tool Coder, Inspector, Shipper) plus end-to-end golden prompts. CI runs the harness on every PR; production deploys block on regressions below a configurable pass-rate threshold.

## Public API surface

- TBD
