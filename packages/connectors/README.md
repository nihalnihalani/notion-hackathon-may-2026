# @forge/connectors

First-party connector SDKs that generated agents import for outbound calls to third-party services. Each connector is a thin, typed, OAuth-scope-minimized wrapper with consistent retry, rate-limit, and timeout semantics so generated TypeScript stays small and predictable.

## Public API surface

- TBD
