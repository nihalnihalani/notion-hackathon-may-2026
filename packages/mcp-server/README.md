# @forge/mcp-server

Exposes Forge as a Model Context Protocol (MCP) server so external agents (Claude Desktop, Cursor, other LLM clients) can invoke `forge.generate`, list a workspace's generated agents, and retrieve build logs without going through the Notion UI surface.

## Public API surface

- TBD
