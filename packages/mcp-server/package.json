{
  "name": "@forge/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "Forge exposed as an MCP server so external agents (Claude Desktop, Cursor, etc.) can run forge.generate.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "2.1.8"
  }
}
